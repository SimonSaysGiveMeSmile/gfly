import { HowBody } from "./HowBody";

export const metadata = {
  title: "How it works",
  description: "How a complete fly brain fits in a browser tab, with the numbers.",
};

export default function HowItWorks() {
  return <HowBody />;
}
